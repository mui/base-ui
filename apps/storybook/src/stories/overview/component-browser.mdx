import { Meta } from '@storybook/addon-docs/blocks';
import { ComponentBrowser } from '../shared/ComponentBrowser';

<Meta title="Overview/Component browser" />

# Component browser

Every Base UI component documented in this Storybook, grouped by category — a browsable index
in the spirit of [coss.com/ui](https://coss.com/ui). Type to filter by name or description, and
click a card to open that component's docs page, which leads with its primary story.

<ComponentBrowser />
