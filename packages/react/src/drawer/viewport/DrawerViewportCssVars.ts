export enum DrawerViewportCssVars {
  /**
   * The software keyboard inset measured from the bottom edge of the layout viewport, set by `Drawer.VirtualKeyboardProvider`.
   * @type {CSS length}
   */
  keyboardInset = '--drawer-keyboard-inset',
}
