export enum DraggablePreviewCssVars {
  /**
   * The width of the element the drag was lifted from.
   * @type {number}
   */
  dragSourceWidth = '--drag-source-width',
  /**
   * The height of the element the drag was lifted from.
   * @type {number}
   */
  dragSourceHeight = '--drag-source-height',
}
