export enum DateFieldSectionDataAttributes {
  /**
   * Index of the section in the field.
   */
  sectionIndex = 'data-section-index',
  /**
   * Present when the section value is empty.
   */
  empty = 'data-empty',
}
