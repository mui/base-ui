export enum DateFieldClearDataAttributes {
  /**
   * Present when the button is disabled.
   */
  disabled = 'data-disabled',
  /**
   * Present when the field value is empty (all sections are empty).
   */
  empty = 'data-empty',
}
