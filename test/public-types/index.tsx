import * as React from 'react';
import { type CheckboxFieldProps } from './checkbox-field';

export const MyCheckbox = React.forwardRef<HTMLButtonElement, CheckboxFieldProps>(() => {
  return <div>helo</div>;
});
