import { releases } from 'docs/src/data/releases';

export const version = 'v1.0.0-rc.1';
export const release = releases.find((release) => release.version === version);

# {version} release notes

<Subtitle>{release?.date}</Subtitle>
<Meta name="description" content={`${version} release notes. ${release?.date}`} />

This release contains the same code as v1.0.0.
Please refer to that version to see the changes.
