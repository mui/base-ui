import { createDemoWithVariants } from 'docs/src/utils/createDemo';
import CssModules from './css-modules';

export const DemoViewTransitionList = createDemoWithVariants(import.meta.url, {
  CssModules,
});
