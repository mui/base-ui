# Filter Menu

<Subtitle>A searchable menu for finding and running actions in a dropdown.</Subtitle>
<Meta
  name="description"
  content="A high-quality, unstyled React menu component with a built-in input for filtering actions."
/>

import { DemoFilterMenuHero } from './demos/hero';
import { DemoFilterMenuCommandPalette } from './demos/command-palette';

<DemoFilterMenuHero />

## Usage guidelines

- **Use for searchable actions**: Choose `FilterMenu` when users need to find an action. Use [Menu](/react/components/menu) for a short list that does not need search.
- **Use Combobox for values**: Choose [Combobox](/react/components/combobox) when users are selecting a value rather than running an action.
- **Name the filter input**: Give `<FilterMenu.Input>` an accessible name with `aria-label`, `aria-labelledby`, or a visible native `<label>`.

## Anatomy

Import the component and assemble its parts:

```jsx title="Anatomy"
import { FilterMenu } from '@base-ui/react/filter-menu';

<FilterMenu.Root>
  <FilterMenu.Trigger>Actions</FilterMenu.Trigger>
  <FilterMenu.Portal>
    <FilterMenu.Backdrop />
    <FilterMenu.Positioner>
      <FilterMenu.Popup>
        <FilterMenu.Input aria-label="Filter actions" />
        <FilterMenu.Clear aria-label="Clear filter" />
        <FilterMenu.Empty>No actions found.</FilterMenu.Empty>
        <FilterMenu.List>
          <FilterMenu.Item>Edit</FilterMenu.Item>
          <FilterMenu.Item>Duplicate</FilterMenu.Item>
        </FilterMenu.List>
      </FilterMenu.Popup>
    </FilterMenu.Positioner>
  </FilterMenu.Portal>
</FilterMenu.Root>;
```

`FilterMenu` supports checkbox items, radio groups, links, groups, and submenus like [Menu](/react/components/menu).

## Examples

### Command palette

Combine `FilterMenu` with [Dialog](/react/components/dialog) to create a command palette.
The `inline` prop renders the list directly inside the dialog without the menu's own popup parts; specify `open` together with it so the list is considered visible.
`autoHighlight="always"` keeps the first match highlighted so Enter always runs a command.

<DemoFilterMenuCommandPalette />

### Filtering items

Items match their rendered text by default. Use `label` to provide explicit searchable text and `keywords` for alternate terms.

```jsx title="Labels and keywords"
<FilterMenu.List>
  <FilterMenu.Item keywords={['edit name']}>Rename</FilterMenu.Item>
  <FilterMenu.Item label="Move to folder" keywords={['organize']}>
    Move
  </FilterMenu.Item>
</FilterMenu.List>
```

Groups with no matching items are hidden, including their labels. `FilterMenu.Empty` is shown when no items match.

### Custom matching

Use `filter` to replace the default case-insensitive substring matching:

```jsx title="Custom filter"
<FilterMenu.Root filter={(itemText, query) => itemText.startsWith(query)}>
  {/* menu parts */}
</FilterMenu.Root>
```

When supplied, the custom filter replaces keyword matching.

### Filterable submenus

Use `<FilterMenu.SubmenuRoot>` when a submenu needs its own search input.
For a submenu without one, use `<Menu.SubmenuRoot>` from [Menu](/react/components/menu) together with `<FilterMenu.SubmenuTrigger>` and plain Menu parts for its popup.

```jsx title="A filterable submenu"
<FilterMenu.SubmenuRoot>
  <FilterMenu.SubmenuTrigger>More actions</FilterMenu.SubmenuTrigger>
  <FilterMenu.Portal>
    <FilterMenu.Positioner>
      <FilterMenu.Popup>
        <FilterMenu.Input aria-label="Filter more actions" />
        <FilterMenu.List>
          <FilterMenu.Item>Share</FilterMenu.Item>
          <FilterMenu.Item>Move</FilterMenu.Item>
        </FilterMenu.List>
      </FilterMenu.Popup>
    </FilterMenu.Positioner>
  </FilterMenu.Portal>
</FilterMenu.SubmenuRoot>
```

## API reference

import { TypesFilterMenu } from './types';

### Root

<TypesFilterMenu.Root />

### Trigger

<TypesFilterMenu.Trigger />

### Portal

<TypesFilterMenu.Portal />

### Backdrop

<TypesFilterMenu.Backdrop />

### Positioner

<TypesFilterMenu.Positioner />

### Popup

<TypesFilterMenu.Popup />

### Input

<TypesFilterMenu.Input />

### Clear

<TypesFilterMenu.Clear />

### Empty

<TypesFilterMenu.Empty />

### List

<TypesFilterMenu.List />

### Viewport

<TypesFilterMenu.Viewport />

### Arrow

<TypesFilterMenu.Arrow />

### Item

<TypesFilterMenu.Item />

### LinkItem

<TypesFilterMenu.LinkItem />

### SubmenuRoot

<TypesFilterMenu.SubmenuRoot />

### SubmenuTrigger

<TypesFilterMenu.SubmenuTrigger />

### Group

<TypesFilterMenu.Group />

### GroupLabel

<TypesFilterMenu.GroupLabel />

### RadioGroup

<TypesFilterMenu.RadioGroup />

### RadioItem

<TypesFilterMenu.RadioItem />

### RadioItemIndicator

<TypesFilterMenu.RadioItemIndicator />

### CheckboxItem

<TypesFilterMenu.CheckboxItem />

### CheckboxItemIndicator

<TypesFilterMenu.CheckboxItemIndicator />

### Separator

<TypesFilterMenu.Separator />

## createHandle

<TypesFilterMenu.createHandle />

[//]: # '@exclude-table-of-contents'

### Handle

<TypesFilterMenu.Handle />

export const metadata = {
  keywords: [
    'React Filter Menu',
    'Searchable Menu',
    'Filterable Menu',
    'Command Menu',
    'Action Search',
    'Search Menu',
    'Keyboard Navigation Menu',
    'Accessible React Menu',
    'Headless React Components',
    'Base UI',
  ],
};
