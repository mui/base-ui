# Password Toggle Field

[//]: types.ts '<-- Autogenerated By (do not edit the following markdown directly), run: pnpm docs:validate "(docs)/react/components/password-toggle-field"'

## API Reference

### Root

Groups the password input and its visibility toggle.
Renders a `<div>` element.

**Root Props:**

| Prop            | Type                                                                                                 | Default | Description                                                                                                                                                                                   |
| :-------------- | :--------------------------------------------------------------------------------------------------- | :------ | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| defaultVisible  | `boolean`                                                                                            | `false` | Whether the password is initially revealed as plain text. To render a controlled password toggle field, use the `visible` prop instead.                                                       |
| onVisibleChange | `((visible: boolean, eventDetails: PasswordToggleFieldPreview.Root.ChangeEventDetails) => void)`     | -       | Callback fired when the password visibility changes.                                                                                                                                          |
| visible         | `boolean`                                                                                            | -       | Whether the password is currently revealed as plain text. To render an uncontrolled password toggle field, use the `defaultVisible` prop instead.                                             |
| disabled        | `boolean`                                                                                            | `false` | Whether the component should ignore user interaction.&#xA;Applies to both the input and the toggle.                                                                                           |
| className       | `string \| ((state: PasswordToggleFieldRootState) => string \| undefined)`                           | -       | CSS class applied to the element, or a function that&#xA;returns a class based on the component's state.                                                                                      |
| style           | `React.CSSProperties \| ((state: PasswordToggleFieldRootState) => React.CSSProperties \| undefined)` | -       | Style applied to the element, or a function that&#xA;returns a style object based on the component's state.                                                                                   |
| render          | `ReactElement \| ((props: HTMLProps, state: PasswordToggleFieldRootState) => ReactElement)`          | -       | Allows you to replace the component's HTML element&#xA;with a different tag, or compose it with another component. Accepts a `ReactElement` or a function that returns the element to render. |

**Root Data Attributes:**

| Attribute     | Type | Description                         |
| :------------ | :--- | :---------------------------------- |
| data-disabled | -    | Present when the field is disabled. |

### Root.Props

Re-export of [Root](#root) props.

### Root.State

```typescript
type PasswordToggleFieldPreviewRootState = {
  /** Whether the field should ignore user interaction. */
  disabled: boolean;
};
```

### Root.ChangeEventReason

```typescript
type PasswordToggleFieldPreviewRootChangeEventReason = 'none';
```

### Root.ChangeEventDetails

```typescript
type PasswordToggleFieldPreviewRootChangeEventDetails = {
  /** The reason for the event. */
  reason: 'none';
  /** The native event associated with the custom event. */
  event: Event;
  /** Cancels Base UI from handling the event. */
  cancel: () => void;
  /** Allows the event to propagate in cases where Base UI will stop the propagation. */
  allowPropagation: () => void;
  /** Indicates whether the event has been canceled. */
  isCanceled: boolean;
  /** Indicates whether the event is allowed to propagate. */
  isPropagationAllowed: boolean;
  /** The element that triggered the event, if applicable. */
  trigger: Element | undefined;
};
```

### Input

The password input. Its `type` toggles between `password` and `text`, and it works with
[Field](https://base-ui.com/react/components/field) for labeling and validation.
Renders an `<input>` element.

**Input Props:**

| Prop         | Type                                                                                                  | Default | Description                                                                                                                                                                                   |
| :----------- | :---------------------------------------------------------------------------------------------------- | :------ | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| defaultValue | `string \| number \| string[]`                                                                        | -       | The default value of the input. Use when uncontrolled.                                                                                                                                        |
| className    | `string \| ((state: PasswordToggleFieldInputState) => string \| undefined)`                           | -       | CSS class applied to the element, or a function that&#xA;returns a class based on the component's state.                                                                                      |
| style        | `React.CSSProperties \| ((state: PasswordToggleFieldInputState) => React.CSSProperties \| undefined)` | -       | Style applied to the element, or a function that&#xA;returns a style object based on the component's state.                                                                                   |
| render       | `ReactElement \| ((props: HTMLProps, state: PasswordToggleFieldInputState) => ReactElement)`          | -       | Allows you to replace the component's HTML element&#xA;with a different tag, or compose it with another component. Accepts a `ReactElement` or a function that returns the element to render. |

**Input Data Attributes:**

| Attribute     | Type | Description                                                                 |
| :------------ | :--- | :-------------------------------------------------------------------------- |
| data-disabled | -    | Present when the field is disabled.                                         |
| data-valid    | -    | Present when the input is in a valid state (when wrapped in Field.Root).    |
| data-invalid  | -    | Present when the input is in an invalid state (when wrapped in Field.Root). |
| data-dirty    | -    | Present when the input's value has changed (when wrapped in Field.Root).    |
| data-touched  | -    | Present when the input has been touched (when wrapped in Field.Root).       |
| data-filled   | -    | Present when the input is filled (when wrapped in Field.Root).              |
| data-focused  | -    | Present when the input is focused (when wrapped in Field.Root).             |

### Input.Props

Re-export of [Input](#input) props.

### Input.State

```typescript
type PasswordToggleFieldPreviewInputState = {
  /** Whether the component should ignore user interaction. */
  disabled: boolean;
  /** Whether the field has been touched. */
  touched: boolean;
  /** Whether the field value has changed from its initial value. */
  dirty: boolean;
  /** Whether the field is valid. */
  valid: boolean | null;
  /** Whether the field has a value. */
  filled: boolean;
  /** Whether the field is focused. */
  focused: boolean;
};
```

### Toggle

A button that toggles the password's visibility.
Renders a `<button>` element.

**Toggle Props:**

| Prop         | Type                                                                                                   | Default | Description                                                                                                                                                                                   |
| :----------- | :----------------------------------------------------------------------------------------------------- | :------ | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| nativeButton | `boolean`                                                                                              | `true`  | Whether the component renders a native `<button>` element when replacing it&#xA;via the `render` prop.&#xA;Set to `false` if the rendered element is not a button (for example, `<div>`).     |
| className    | `string \| ((state: PasswordToggleFieldToggleState) => string \| undefined)`                           | -       | CSS class applied to the element, or a function that&#xA;returns a class based on the component's state.                                                                                      |
| style        | `React.CSSProperties \| ((state: PasswordToggleFieldToggleState) => React.CSSProperties \| undefined)` | -       | Style applied to the element, or a function that&#xA;returns a style object based on the component's state.                                                                                   |
| render       | `ReactElement \| ((props: HTMLProps, state: PasswordToggleFieldToggleState) => ReactElement)`          | -       | Allows you to replace the component's HTML element&#xA;with a different tag, or compose it with another component. Accepts a `ReactElement` or a function that returns the element to render. |

**Toggle Data Attributes:**

| Attribute     | Type | Description                                                                  |
| :------------ | :--- | :--------------------------------------------------------------------------- |
| data-pressed  | -    | Present when the toggle is pressed (the password is revealed as plain text). |
| data-disabled | -    | Present when the toggle is disabled.                                         |

### Toggle.Props

Re-export of [Toggle](#toggle) props.

### Toggle.State

```typescript
type PasswordToggleFieldPreviewToggleState = {
  /** Whether the toggle is pressed (the password is revealed as plain text). */
  pressed: boolean;
  /** Whether the toggle should ignore user interaction. */
  disabled: boolean;
};
```

## Additional Types

### PasswordToggleFieldInputProps

```typescript
type PasswordToggleFieldInputProps = {
  /** The default value of the input. Use when uncontrolled. */
  defaultValue?: string | number | string[];
  /**
   * CSS class applied to the element, or a function that
   * returns a class based on the component's state.
   */
  className?: string | ((state: PasswordToggleFieldInputState) => string | undefined);
  /**
   * Allows you to replace the component's HTML element
   * with a different tag, or compose it with another component.
   *
   * Accepts a `ReactElement` or a function that returns the element to render.
   */
  render?:
    | ReactElement
    | ((props: HTMLProps, state: PasswordToggleFieldInputState) => ReactElement);
  /**
   * Style applied to the element, or a function that
   * returns a style object based on the component's state.
   */
  style?:
    | React.CSSProperties
    | ((state: PasswordToggleFieldInputState) => React.CSSProperties | undefined);
};
```

### PasswordToggleFieldInputState

```typescript
type PasswordToggleFieldInputState = {
  /** Whether the component should ignore user interaction. */
  disabled: boolean;
  /** Whether the field has been touched. */
  touched: boolean;
  /** Whether the field value has changed from its initial value. */
  dirty: boolean;
  /** Whether the field is valid. */
  valid: boolean | null;
  /** Whether the field has a value. */
  filled: boolean;
  /** Whether the field is focused. */
  focused: boolean;
};
```

### PasswordToggleFieldRootChangeEventReason

```typescript
type PasswordToggleFieldRootChangeEventReason = 'none';
```

### PasswordToggleFieldRootProps

```typescript
type PasswordToggleFieldRootProps = {
  /**
   * Whether the password is currently revealed as plain text.
   *
   * To render an uncontrolled password toggle field, use the `defaultVisible` prop instead.
   */
  visible?: boolean;
  /**
   * Whether the password is initially revealed as plain text.
   *
   * To render a controlled password toggle field, use the `visible` prop instead.
   * @default false
   */
  defaultVisible?: boolean;
  /** Callback fired when the password visibility changes. */
  onVisibleChange?: (
    visible: boolean,
    eventDetails: PasswordToggleFieldRoot.ChangeEventDetails,
  ) => void;
  /**
   * Whether the component should ignore user interaction.
   * Applies to both the input and the toggle.
   * @default false
   */
  disabled?: boolean;
  /**
   * CSS class applied to the element, or a function that
   * returns a class based on the component's state.
   */
  className?: string | ((state: PasswordToggleFieldRootState) => string | undefined);
  /**
   * Allows you to replace the component's HTML element
   * with a different tag, or compose it with another component.
   *
   * Accepts a `ReactElement` or a function that returns the element to render.
   */
  render?: ReactElement | ((props: HTMLProps, state: PasswordToggleFieldRootState) => ReactElement);
  /**
   * Style applied to the element, or a function that
   * returns a style object based on the component's state.
   */
  style?:
    | React.CSSProperties
    | ((state: PasswordToggleFieldRootState) => React.CSSProperties | undefined);
};
```

### PasswordToggleFieldRootState

```typescript
type PasswordToggleFieldRootState = {
  /** Whether the field should ignore user interaction. */
  disabled: boolean;
};
```

### PasswordToggleFieldToggleProps

```typescript
type PasswordToggleFieldToggleProps = {
  /**
   * Whether the component renders a native `<button>` element when replacing it
   * via the `render` prop.
   * Set to `false` if the rendered element is not a button (for example, `<div>`).
   * @default true
   */
  nativeButton?: boolean;
  /**
   * CSS class applied to the element, or a function that
   * returns a class based on the component's state.
   */
  className?: string | ((state: PasswordToggleFieldToggleState) => string | undefined);
  /**
   * Allows you to replace the component's HTML element
   * with a different tag, or compose it with another component.
   *
   * Accepts a `ReactElement` or a function that returns the element to render.
   */
  render?:
    | ReactElement
    | ((props: HTMLProps, state: PasswordToggleFieldToggleState) => ReactElement);
  /**
   * Style applied to the element, or a function that
   * returns a style object based on the component's state.
   */
  style?:
    | React.CSSProperties
    | ((state: PasswordToggleFieldToggleState) => React.CSSProperties | undefined);
};
```

### PasswordToggleFieldToggleState

```typescript
type PasswordToggleFieldToggleState = {
  /** Whether the toggle is pressed (the password is revealed as plain text). */
  pressed: boolean;
  /** Whether the toggle should ignore user interaction. */
  disabled: boolean;
};
```

## Export Groups

- `PasswordToggleFieldPreview.Root`: `PasswordToggleFieldPreview.Root`, `PasswordToggleFieldPreview.Root.State`, `PasswordToggleFieldPreview.Root.Props`, `PasswordToggleFieldPreview.Root.ChangeEventReason`, `PasswordToggleFieldPreview.Root.ChangeEventDetails`
- `PasswordToggleFieldPreview.Input`: `PasswordToggleFieldPreview.Input`, `PasswordToggleFieldPreview.Input.State`, `PasswordToggleFieldPreview.Input.Props`
- `PasswordToggleFieldPreview.Toggle`: `PasswordToggleFieldPreview.Toggle`, `PasswordToggleFieldPreview.Toggle.State`, `PasswordToggleFieldPreview.Toggle.Props`
- `Default`: `PasswordToggleFieldRootState`, `PasswordToggleFieldRootProps`, `PasswordToggleFieldRootChangeEventReason`, `PasswordToggleFieldRootChangeEventDetails`, `PasswordToggleFieldInputState`, `PasswordToggleFieldInputProps`, `PasswordToggleFieldToggleState`, `PasswordToggleFieldToggleProps`

## Canonical Types

Maps `Canonical`: `Alias` — Use Canonical when its namespace is already imported; otherwise use Alias.

- `PasswordToggleFieldRoot.ChangeEventDetails`: `PasswordToggleFieldRootChangeEventDetails`
