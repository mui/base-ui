# Date Time Field

<Subtitle>An easily stylable date time field component.</Subtitle>

<Meta
  name="description"
  content="A high-quality, unstyled React date time field that is easy to customize."
/>

import { DemoDateTimeFieldHero } from './demos/hero';

<DemoDateTimeFieldHero />

## Anatomy

Import the component and assemble its parts:

```jsx title="Anatomy"
import { DateTimeField } from '@base-ui/react/date-time-field';

<DateTimeField.Root>
  <DateTimeField.Section />
  <DateTimeField.SectionList>
    <DateTimeField.Section />
  </DateTimeField.SectionList>
  <DateTimeField.Clear />
</DateTimeField.Root>;
```

## API reference

<Reference component="DateTimeField" parts="Root" />
<Reference component="DateField" parts="Section, SectionList, Clear" />

## Examples

### Custom format

Use the `format` prop to customize the date and time display format. The format string follows [date-fns format patterns](https://date-fns.org/docs/format).

import { DemoDateTimeFieldCustomFormat } from './demos/custom-format';

<DemoDateTimeFieldCustomFormat />

### Clear button

Wrap the sections in `<DateTimeField.SectionList>` to render the clear button next to them using `<DateTimeField.Clear>`. The `SectionList` groups all sections into a single element so that additional controls can be placed alongside them inside the field.

import { DemoDateTimeFieldClearButton } from './demos/clear-button';

<DemoDateTimeFieldClearButton />

### Validation

Use the `minDate` and `maxDate` props to constrain the allowed date and time range. Combine with `<Field.Error>` to display validation messages.

import { DemoDateTimeFieldValidation } from './demos/validation';

<DemoDateTimeFieldValidation />

### Localization

Use `<TemporalLocaleProvider>` to set the locale and `placeholderGetters` to customize placeholder text.

The `placeholderGetters` prop is a temporary solution. It will no longer be needed once a built-in localization system is implemented.

import { DemoDateTimeFieldLocalization } from './demos/localization';

<DemoDateTimeFieldLocalization />

export const metadata = {
  keywords: ['React Date Time Field', 'Date Time Input', 'Date Time Picker', 'Temporal', 'Base UI'],
};
