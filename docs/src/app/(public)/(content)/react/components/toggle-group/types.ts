import { ToggleGroup } from '@base-ui-components/react/toggle-group';
import { createTypes } from 'docs/src/utils/createTypes';

export const TypesToggleGroup = createTypes(import.meta.url, ToggleGroup);
