# Temporal Locale Provider

<Subtitle>Defines the locale to use in the temporal components.</Subtitle>
<Meta
  name="description"
  content="A temporal locale provider component that defines the locale to use in the temporal components."
/>

<Demo path="./demos/hero" />

## API reference

Import the component plus the `date-fns` library locale and wrap them around your app.

If every temporal component in your app shares the same locale, you should use the provider once at the root of your application.

```jsx title="Anatomy"
import { fr } from 'date-fns/locale/fr';
import { UnstableTemporalLocaleProvider as TemporalLocaleProvider } from '@base-ui-components/react/temporal-locale-provider';

<TemporalLocaleProvider locale={fr}>
  {/* Your app or a group of components */}
</TemporalLocaleProvider>;
```

<Reference component="TemporalLocaleProvider" />

## Examples

### Use without explicit locale

If you don't provide a locale, the provider will use the default `en-US` locale.

```jsx title="Default locale"
import * as React from 'react';
import { Calendar } from '@base-ui-components/react/calendar';
import { UnstableTemporalLocaleProvider as TemporalLocaleProvider } from '@base-ui-components/react/temporal-locale-provider';

function App() {
  const [value, setValue] = (React.useState < Date) | (null > null);

  return (
    <TemporalLocaleProvider>
      <Calendar.Root value={value} onChange={setValue} minDate={new Date('2025-01-01')}>
        {/** Content of the calendar */}
      </Calendar.Root>
    </TemporalLocaleProvider>
  );
}
```
