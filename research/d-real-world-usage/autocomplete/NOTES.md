# autocomplete — Phase D compressed-pass notes

- **Searched**: deduped-repos.json samplePaths for a path segment matching "autocomplete" (and its no-hyphen/PascalCase variants) across all 771 deduped repos; repos.json description field for the literal substring "autocomplete"; the foundVia tags for a cached code-import-*-autocomplete-* search (none exists - only combobox/dialog/menu/select/popover have dedicated cached subpath searches).
- **Why empty**: No repo in the corpus has a sampled file path segment or a repo description that names "autocomplete" specifically without also matching an unrelated word or an already-covered component's file (checked and rejected as false positives where a coincidental hit existed). No cached GitHub code search ever targeted "@base-ui/react/autocomplete" or "@base-ui-components/react/autocomplete" directly.
- **Future full pass should run**: Run GitHub code search for "from '@base-ui/react/autocomplete'" and "from '@base-ui-components/react/autocomplete'", scoped and unscoped (per the methodology's p1/scoped-toprepos pattern used for select/menu/dialog/popover), then re-run deduped-repos.json aggregation including this subpath in foundVia; also grep the 111 oss-design-system repos' file trees directly (not just samplePaths) since a full design-system registry plausibly implements this component even where the corpus-only signal was too weak to cite honestly here.

- **2026-07 update (registry-tree mining, not a code search)**: a targeted follow-up directly fetched the recursive git-tree listing (`gh api repos/OWNER/REPO/git/trees/HEAD?recursive=1`) of 6 Base UI component registries/design systems (cloudflare/kumo, borabaloglu/9ui, patrick-xin/lumi-ui, cosscom/coss, keenthemes/reui, plus WordPress/gutenberg's `packages/ui`) rather than relying on GitHub code search. This surfaced verifiable file paths for this component in most of those registries; 3 were appended to `candidates.json` (schema §11.3, `pinned: false`, `diversityTags` includes `"registry-tree-mined-2026-07"`) and written up in the Storybook MDX "In the wild" section. This does not fully close the gap the "Future full pass" note below describes (no GitHub code search was run, and non-registry production usage is still unverified), but it directly executes the "grep the oss-design-system repos' file trees directly" suggestion.

- **2026-07-07/08 full pass**: ran the "Future full pass" this note called for. Two GitHub code searches: `"@base-ui/react/autocomplete"` (1032 total hits) and (shared budget) confirmed via the field/form passes that the old package name yields negligible additional autocomplete-specific signal beyond what registry mining already found. Combined with the 8 cached registry trees (`_cache/registry-trees/*.txt`, now including shadcn-ui/ui — which has **no** autocomplete entry at all, confirming shadcn hasn't built this component) this surfaced 8 new genuine candidates: langgenius/dify (148k stars), PostHog/posthog ("Quill" design system, 35k), baptisteArno/typebot.io (10k), gitbutlerapp/gitbutler (21k, command-palette composed from Autocomplete+Dialog+ScrollArea), keenthemes/reui (full Backdrop+Arrow anatomy, multi-skin theming), and the near-duplicate trio pingdotgg/t3code + different-ai/openwork + cosscom/coss (same shadcn-derived wrapper). All permalinks re-pinned from `blob/HEAD/` to the resolved commit SHA. `candidates.json` now has 11 entries, `ranked.json` has the top 10 with full scores + mandatory `rankRationale`, and `examples.md` covers the top 5 in prose plus a table for 6-10. See `examples.md` for the write-up and citations.
