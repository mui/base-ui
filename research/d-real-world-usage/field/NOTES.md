# field — Phase D compressed-pass notes

- **Searched**: deduped-repos.json samplePaths for a path segment matching "field" (and its no-hyphen/PascalCase variants) across all 771 deduped repos; repos.json description field for the literal substring "field"; the foundVia tags for a cached code-import-*-field-* search (none exists - only combobox/dialog/menu/select/popover have dedicated cached subpath searches).
- **Why empty**: No repo in the corpus has a sampled file path segment or a repo description that names "field" specifically without also matching an unrelated word or an already-covered component's file (checked and rejected as false positives where a coincidental hit existed). No cached GitHub code search ever targeted "@base-ui/react/field" or "@base-ui-components/react/field" directly.
- **Future full pass should run**: Run GitHub code search for "from '@base-ui/react/field'" and "from '@base-ui-components/react/field'", scoped and unscoped (per the methodology's p1/scoped-toprepos pattern used for select/menu/dialog/popover), then re-run deduped-repos.json aggregation including this subpath in foundVia; also grep the 111 oss-design-system repos' file trees directly (not just samplePaths) since a full design-system registry plausibly implements this component even where the corpus-only signal was too weak to cite honestly here.

- **2026-07 update (registry-tree mining, not a code search)**: a targeted follow-up directly fetched the recursive git-tree listing (`gh api repos/OWNER/REPO/git/trees/HEAD?recursive=1`) of 6 Base UI component registries/design systems (cloudflare/kumo, borabaloglu/9ui, patrick-xin/lumi-ui, cosscom/coss, keenthemes/reui, plus WordPress/gutenberg's `packages/ui`) rather than relying on GitHub code search. This surfaced verifiable file paths for this component in most of those registries; 3 were appended to `candidates.json` (schema §11.3, `pinned: false`, `diversityTags` includes `"registry-tree-mined-2026-07"`) and written up in the Storybook MDX "In the wild" section. This does not fully close the gap the "Future full pass" note below describes (no GitHub code search was run, and non-registry production usage is still unverified), but it directly executes the "grep the oss-design-system repos' file trees directly" suggestion.

- **2026-07-07/08 full pass**: ran the "Future full pass" this note called for — GitHub code search for `"@base-ui/react/field"` (1420 total hits) and the legacy `"@base-ui-components/react/field"` (377 total hits), plus a re-grep of the cached registry trees (now 8 repos, including shadcn-ui/ui). This surfaced 7 new genuine candidates: InseeFr/Pogues (react-hook-form `FieldError` bridging), baptisteArno/typebot.io, raystack/apsara (CSS Modules + context-shared state), usekaneo/kaneo, Codennnn/Green-Wall (still on the legacy package name), plus corrections of pinned permalinks for kumo/gutenberg/lumi-ui. **Important correction**: both keenthemes/reui's and shadcn-ui/ui's `ui/field.tsx` were inspected and found to be plain-HTML (`<fieldset>`/`<div role="group">`) re-implementations with **no** `@base-ui/react/field` import at all, despite both genuinely wrapping other Base UI components (Select, Autocomplete) elsewhere — see `examples.md` for the write-up; they are recorded as verified non-candidates, not silently omitted. `candidates.json` now has 10 genuine entries, `ranked.json` ranks all 10 with mandatory `rankRationale`.
