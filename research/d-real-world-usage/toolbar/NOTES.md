# toolbar — Phase D compressed-pass notes

- **Searched**: deduped-repos.json samplePaths for a path segment matching "toolbar" across all 771 deduped repos (5 raw hits found: settings-popover.tsx, top-toolbar.tsx, formatting-toolbar.tsx, theme-name.tsx x2); repos.json description field for "toolbar"; foundVia tags for a cached code-import-*-toolbar-* search (none exists).
- **Why empty**: All 5 raw path hits inspected individually and rejected as false positives: each file is a popover/menu/select component (settings-popover.tsx, formatting-toolbar.tsx, theme-name.tsx, top-toolbar.tsx) that merely lives inside a directory literally named toolbar/ or Toolbar/ - none of the files themselves are Base UI's Toolbar primitive, and none were found via a toolbar-specific cached search. No genuine component-level evidence survived inspection.
- **Future full pass should run**: Run GitHub code search for "from '@base-ui/react/toolbar'" and "from '@base-ui-components/react/toolbar'", scoped and unscoped; cross-check the 111 oss-design-system repos' full file trees (not just cached samplePaths) since Toolbar is a common composite in comprehensive registries even when no single cached search surfaced it.

- **2026-07 update (registry-tree mining, not a code search)**: a targeted follow-up directly fetched the recursive git-tree listing (`gh api repos/OWNER/REPO/git/trees/HEAD?recursive=1`) of 6 Base UI component registries/design systems (cloudflare/kumo, borabaloglu/9ui, patrick-xin/lumi-ui, cosscom/coss, keenthemes/reui, plus WordPress/gutenberg's `packages/ui`) rather than relying on GitHub code search. This surfaced verifiable file paths for this component in most of those registries; 3 were appended to `candidates.json` (schema §11.3, `pinned: false`, `diversityTags` includes `"registry-tree-mined-2026-07"`) and written up in the Storybook MDX "In the wild" section. This does not fully close the gap the "Future full pass" note below describes (no GitHub code search was run, and non-registry production usage is still unverified), but it directly executes the "grep the oss-design-system repos' file trees directly" suggestion.
