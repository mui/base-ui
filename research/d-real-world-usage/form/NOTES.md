# form — Phase D compressed-pass notes

- **Searched**: deduped-repos.json samplePaths for a path segment matching "form" (and its no-hyphen/PascalCase variants) across all 771 deduped repos; repos.json description field for the literal substring "form"; the foundVia tags for a cached code-import-*-form-* search (none exists - only combobox/dialog/menu/select/popover have dedicated cached subpath searches).
- **Why empty**: No repo in the corpus has a sampled file path segment or a repo description that names "form" specifically without also matching an unrelated word or an already-covered component's file (checked and rejected as false positives where a coincidental hit existed). No cached GitHub code search ever targeted "@base-ui/react/form" or "@base-ui-components/react/form" directly.
- **Future full pass should run**: Run GitHub code search for "from '@base-ui/react/form'" and "from '@base-ui-components/react/form'", scoped and unscoped (per the methodology's p1/scoped-toprepos pattern used for select/menu/dialog/popover), then re-run deduped-repos.json aggregation including this subpath in foundVia; also grep the 111 oss-design-system repos' file trees directly (not just samplePaths) since a full design-system registry plausibly implements this component even where the corpus-only signal was too weak to cite honestly here.

- **2026-07 update (registry-tree mining, not a code search)**: a targeted follow-up directly fetched the recursive git-tree listing (`gh api repos/OWNER/REPO/git/trees/HEAD?recursive=1`) of 6 Base UI component registries/design systems (cloudflare/kumo, borabaloglu/9ui, patrick-xin/lumi-ui, cosscom/coss, keenthemes/reui, plus WordPress/gutenberg's `packages/ui`) rather than relying on GitHub code search. This surfaced verifiable file paths for this component in most of those registries; 3 were appended to `candidates.json` (schema §11.3, `pinned: false`, `diversityTags` includes `"registry-tree-mined-2026-07"`) and written up in the Storybook MDX "In the wild" section. This does not fully close the gap the "Future full pass" note below describes (no GitHub code search was run, and non-registry production usage is still unverified), but it directly executes the "grep the oss-design-system repos' file trees directly" suggestion.

- **2026-07-07/08 full pass**: ran the "Future full pass" this note called for — GitHub code search for `"@base-ui/react/form"` (524 total hits, heavily dominated by mui/base-ui's own docs/tests, which were excluded per the corpus-building rule). This surfaced 3 new genuine candidates (langgenius/dify, nauvalazhar/selia, usekaneo/kaneo) plus richer demo files for the existing lumi-ui entry (form-rhf.tsx, form-zod.tsx, form-native-submit.tsx). **Important correction, not a gap**: Form is real-world-thin by nature — it renders no visual chrome, so most "wrappers" are one-line re-exports, and two apparent hits turned out to be false positives on inspection: 9ui's and reui's `components/ui/form.tsx` are the classic react-hook-form `FormProvider`-based shadcn pattern with **no** `@base-ui/react/form` import anywhere in the file. shadcn-ui/ui itself has **no** Form component at all in its Base UI ('base') registry — its canonical `login-form.tsx` block deliberately uses a plain native `<form>` with `Field`/`FieldGroup` instead. All three are recorded as verified non-candidates in `candidates.json` (not silently dropped) and written up in `examples.md`. Final count: 6 genuine ranked candidates (below the "top 6-10" target, honestly reported — Form's own surface area is simply too small to source more than that from real usage; the shortfall is a property of the component, not of the search effort).
