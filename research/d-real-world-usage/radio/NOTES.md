# radio — Phase D compressed-pass notes

- **Searched**: deduped-repos.json samplePaths for a path segment matching "radio" (and its no-hyphen/PascalCase variants) across all 771 deduped repos; repos.json description field for the literal substring "radio"; the foundVia tags for a cached code-import-*-radio-* search (none exists - only combobox/dialog/menu/select/popover have dedicated cached subpath searches).
- **Why empty**: No repo in the corpus has a sampled file path segment or a repo description that names "radio" specifically without also matching an unrelated word or an already-covered component's file (checked and rejected as false positives where a coincidental hit existed). No cached GitHub code search ever targeted "@base-ui/react/radio" or "@base-ui-components/react/radio" directly.
- **Future full pass should run**: Run GitHub code search for "from '@base-ui/react/radio'" and "from '@base-ui-components/react/radio'", scoped and unscoped (per the methodology's p1/scoped-toprepos pattern used for select/menu/dialog/popover), then re-run deduped-repos.json aggregation including this subpath in foundVia; also grep the 111 oss-design-system repos' file trees directly (not just samplePaths) since a full design-system registry plausibly implements this component even where the corpus-only signal was too weak to cite honestly here.
